let selecteditem=null;//This is a null literal assigned to the variable selecteditem, indicating that it currently has no value or is intentionally empty
console.log(selecteditem);
let selectedresult=null;//This is a null literal assigned to the variable selectedresult, indicating that it currently has no value or is intentionally empty
console.log(selectedresult); 
let result;//This is a variable declaration without an assignment, so result is initialized with the value undefined, which indicates that it has been declared but not assigned a value yet.
console.log(result);//this will print undefined to the console, indicating that the variable result has been declared but not assigned a value.